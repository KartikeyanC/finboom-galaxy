/// <reference types="vite/client" />

/**
 * Build stamp injected by `vite.config.ts` (Stage 5.7). It is the answer to
 * "which version are you looking at?", which is the first question of most
 * support threads and the one users can never answer. `dev` in development.
 */
declare const __BUILD_STAMP__: string;
