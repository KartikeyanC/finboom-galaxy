/**
 * FinRoot motion system — public entry point.
 *
 *   import { ease, duration, fadeUp, logMotion } from "@/animations";
 *
 * Tokens (`motionTokens`), variant presets (`variants`), and the registry /
 * debug / intensity helpers (`registry`). The React components that consume
 * these live in `@/components/motion`.
 */
export * from "./motionTokens";
export * from "./variants";
export * from "./registry";
